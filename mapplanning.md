# Suroi 地图接入规划

## 目标

这次的目标不是缩小地图，也不是改变已有战斗手感，而是：

1. 保持我们现有战斗地图的整体尺度不变。
2. 保持玩家看到的人物大小不变。
3. 保持相机视野和跟随手感不变。
4. 借用 Suroi 的 `fall` 地图素材和风格，做出一张新地图。
5. 新地图在玩家眼里，要和我们现有地图属于同一种“尺度语言”。

当前项目里，这几个约束已经写死在前端里：

- 世界大小是 `2560 x 1600`，见 [battleMapCatalog.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/maps/battleMapCatalog.ts:143)
- 相机缩放是 `1.40`，见 [battleCameraDirector.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/renderer/camera/battleCameraDirector.ts:32)
- 角色碰撞半径是 `18`，见 [constants.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/constants.ts:14)
- 角色贴图缩放是 `1.35`，见 [constants.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/constants.ts:15)

这意味着我们不应该把 Suroi 的原地图“原样搬运”，而应该把它当成一套视觉素材和布局灵感，再重新映射到我们的尺度体系里。

## 先说结论

如果我们希望：

- 地图仍然是 `2560 x 1600`
- 人物看起来和旧地图一样大
- 障碍物和景观看起来也偏大、偏近
- 相机继续用 `zoom 1.4`

那么最合适的做法不是：

- 直接使用 Suroi 的 `fall` 生成逻辑
- 直接使用 Suroi 的原始碰撞数据
- 直接把 `1924 x 1924` 的地图按比例塞进来

而是：

**保留我们自己的地图尺度和相机体系，只借用 Suroi 的视觉主题、对象类型、局部布局结构和素材。**

换句话说，新地图应该是：

**Suroi Fall 风格的 Slay 地图**

而不是：

**把 Suroi Fall 直接缩放后塞进 Slay**

## 为什么不能直接搬

Suroi 的 `fall` 本身不是固定贴图地图，而是一套生成规则。定义在：

- [maps.ts](D:/26Spring/Typesafe/slay/TPsys/reference/suroi/server/src/data/maps.ts:262)

它的基础尺寸是：

- `width: 1924`
- `height: 1924`

见 [maps.ts](D:/26Spring/Typesafe/slay/TPsys/reference/suroi/server/src/data/maps.ts:263)

Suroi 的 `fall` 还依赖它自己的模式定义和素材表：

- [modes.ts](D:/26Spring/Typesafe/slay/TPsys/reference/suroi/common/src/definitions/modes.ts:115)

这套系统和我们当前项目最大的差异有四个：

1. **地图生成思路不同**
   我们当前地图是手写静态布局。Suroi 的 `fall` 是程序生成导向。

2. **碰撞表达不同**
   我们当前前后端基本围绕简单矩形障碍物在工作。Suroi 有更丰富的建筑、树木、河流、地形和命中盒体系。

3. **视觉尺度不同**
   我们当前人物大、障碍物大、镜头近，强调“清楚、厚重、近距离”。Suroi 原版更偏 battle royale 的大地图表达。

4. **玩法空间密度不同**
   我们地图虽然数值上大，但真实可玩区和视野语言更像中小型竞技场。Suroi `fall` 更像开放场景。

所以，如果我们想保持现在这种“人物和掩体都比较大”的感觉，直接按原逻辑搬运，玩家看到的新地图会有两个明显问题：

- 景物显得偏小、偏碎
- 空间显得过空、跑图感更强

## 我们真正要保持的，不是尺寸本身，而是尺度关系

这里最关键的不是“地图宽高数字”，而是下面这组关系：

1. 人物半径相对地图宽度的占比
2. 人物相对掩体的占比
3. 人物相对通道宽度的占比
4. 人物相对建筑入口、树、箱子、石头的占比
5. 相机一屏内看到多少有效战斗空间

只要这组关系不变，玩家就会觉得“这还是同一个游戏的地图”。

从这个角度看，我们接入 Suroi 风格地图时，应该坚持下面几条：

- 世界继续使用 `2560 x 1600`
- 相机继续使用 `zoom 1.4`
- 玩家半径继续接近 `18`
- 玩家 sprite 缩放继续接近 `1.35`
- 主要掩体、树、石头、建筑外轮廓，都按照我们现有地图的阅读尺度放大后再摆放

## 推荐路线

推荐路线很明确：

**不复用 Suroi 的地图生成器，只复用它的素材、主题配色、对象清单、局部布局模式。**

然后在我们自己的地图配置里，手工或半自动做一张新的 `fall-industrial-scale` 地图。

这个路线的优点是：

- 不会破坏我们已有的战斗手感
- 前后端改动最可控
- 可以按我们的竞技节奏去设计通道、掩体和出生点
- 可以保持旧地图和新地图在阅读体验上统一

## 地图设计原则

如果新地图要看起来像 Suroi 的 `fall`，但手感像我们当前地图，那么建议这样设计。

### 1. 只借用 Fall 的视觉语言

保留这些元素：

- 秋季草地色调
- 橙黄棕绿的地表层次
- 树、石头、木箱、南瓜、灌木
- 河边、小路、林间空地
- 偏 rustic 的建筑或障碍物主题

不要直接照搬这些元素：

- 超大面积空地
- 大量远距离跑图空间
- 原版大地图节奏
- 复杂建筑内部结构
- 河流穿越全图的长线地形逻辑

### 2. 把 Fall 做成“竞技场版本”

建议新地图不是开放岛图，而是一个更紧凑的战斗区。可以把整张地图想成：

- 中央主交战区
- 左右两个 flank 区
- 上下两个资源区
- 若干短通道和视线阻断

这样既有 `fall` 的主题感，也能保留我们现在“看得清、打得快、掩体明确”的节奏。

### 3. 保持大物体比例

你提到“人物大小和景观大小都比较大”，这点非常重要。它意味着：

- 树不能做成小点缀，要做成可读的大障碍物
- 石头不能只是碎石贴花，要能承担掩体角色
- 木箱、木栅栏、南瓜堆这些都应该有明确的战术含义
- 建筑不要做成远景装饰，而要么是大轮廓掩体，要么干脆不做

也就是说，素材是 Suroi 的，**尺度语言要是我们的**。

## 实现层面的建议

### 路线 A：最推荐

**把 Suroi 素材搬进我们项目，然后继续用我们当前的 `BattleMapConfig` 思路写新地图。**

这条路里，地图依然是静态配置，只是主题换成 `fall`。

实施方式：

1. 复制或整理 Suroi `fall` 需要用到的素材到我们的前端资源目录。
2. 给每种素材建立一个我们自己的对象定义，例如：
   - `fall-tree-large`
   - `fall-rock-wide`
   - `fall-crate-wood`
   - `fall-pumpkin-clump`
   - `fall-bush-round`
3. 每个对象都配一套我们自己的碰撞尺寸。
4. 在新的地图配置里，按 `2560 x 1600` 世界坐标摆放这些对象。
5. 前端渲染这些对象，后端只吃简化后的碰撞和出生点。

这条路线的核心思想是：

**视觉资源来自 Suroi，玩法空间来自我们自己。**

### 路线 B：次优

**先从 Suroi 的某个生成结果里截取一部分，再人工放大重建。**

也就是：

1. 参考 `fall` 生成后的样子或 `mapview/fall.png`
2. 选一块你喜欢的区域
3. 不直接裁图
4. 而是把那一块的布局关系重新画进我们的 `2560 x 1600` 里
5. 同时把树、石头、路径、掩体做成更大的版本

这个方案能更接近 Suroi 的地图气质，但最后仍然应该落回“静态重建”，而不是直接做贴图裁切。

### 路线 C：不推荐

**直接把 Suroi 的 `fall` 按比例拉伸到 `2560 x 1600`。**

这会有几个问题：

- 原有对象密度会被拉散
- 河流和道路比例会变怪
- 建筑和树木显得过小
- 战斗空间不符合我们当前近景、厚重的阅读习惯

## 前端应该怎么做

### 1. 增加新主题资源

当前资源入口在：

- [constants.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/constants.ts:28)

这里现在是 `industrial` 风格资源路径。接入 `fall` 时，建议新增一套主题资源映射，而不是直接替换旧资源。

建议结构：

```ts
themeId: "fall-large"
```

然后根据 `themeId` 切换：

- floor
- outside
- wall
- crate
- tree
- rock
- bush
- pickup pad visuals

### 2. 把障碍物从“只有 wall/crate”扩展成更多类型

当前 `ArenaObstacle.kind` 只有：

- `wall`
- `crate`

如果要做 Suroi Fall 风格地图，建议扩展出视觉种类，但碰撞仍然先保持简单。

例如：

```ts
kind: "wall" | "crate" | "tree" | "rock" | "bush" | "pumpkin"
```

其中：

- `tree` 和 `rock` 可以有碰撞
- `bush` 可以先做无碰撞或弱碰撞
- `pumpkin` 可以做装饰，或者少量做掩体

前端渲染时根据 `kind` 选材质，后端碰撞依旧可以先统一成 AABB。

### 3. 增加主题化背景层

当前背景和竞技场底板是工业风的，在：

- [arenaBackgroundPresenter.ts](D:/26Spring/Typesafe/slay/TPsys/frontend/src/domains/battle/game/renderer/arena/arenaBackgroundPresenter.ts:26)

如果我们保留旧地图，也要上新地图，建议不要把这个文件写成单一工业风。更好的方式是：

- `industrial` 主题一套背景绘制
- `fall-large` 主题另一套背景绘制

`fall-large` 的背景建议：

- 主地面是秋季草地 tile
- 局部有泥土或小路纹理
- 边缘有树林/暗边压住边界
- 河流如果要上，先做视觉层，后续再决定是否上玩法

### 4. 相机不要动

现阶段不要改：

- `zoom 1.4`
- pointer look-ahead
- 跟随逻辑

我们现在要追求的是“新图接进来仍然像老图”，所以镜头语言应该保持稳定。

## 后端应该怎么做

### 1. 不要试图搬 Suroi 的完整碰撞体系

当前后端地图结构本质上还是简化空间模型。对我们来说最重要的是：

- 世界边界
- 出生点
- 障碍物阻挡
- 拾取点

所以第一版接入新地图时，后端应该继续保持简化：

- 树转成矩形或圆形阻挡
- 石头转成矩形阻挡
- 大建筑先转成几个矩形墙段
- 灌木、落叶、地面装饰不进碰撞
- 河流先不做复杂地形逻辑

### 2. 保持前后端一份地图真相

如果继续用现在这种“前端一份地图、后端一份地图”的写法，那么新图也必须两边都同步改。

这会增加维护成本，但第一版可以接受。

更好的中期目标是：

- 做一份共享地图 JSON
- 前端读渲染字段
- 后端读碰撞和出生点字段

这样新图以后会省很多事。

## 如何让新版人物看起来和旧地图一样大

这部分是本次最核心的问题。

想让玩家觉得“人物还是原来那个大小”，关键不是去改人物，而是要让**环境围绕人物的比例**跟旧图一致。

建议采用下面的标尺：

1. 以当前人物半径 `18` 为基准不动。
2. 以当前主要掩体大小为基准，保留 `48` 到 `64` 的主体障碍物规格。
3. Suroi 素材如果原始视觉较小，就在前端渲染层把 display size 放大。
4. 通道宽度优先按“容纳 2 到 3 个角色并行”的尺度设计，而不是按素材原图尺寸设计。
5. 主要战斗区的掩体间距，优先复用旧图的阅读密度。

可以把它理解成：

**人物不需要为了适配 Suroi 地图而缩放，应该让 Suroi 风格的物体去适配我们的人物尺度。**

## 一个实际可落地的设计方法

如果我们真的开始做这张图，我建议按下面这个顺序。

### 第一步：先不碰后端，只做白盒布局

在 `2560 x 1600` 里先做一张纯白盒地图：

- 中央一块主战区
- 左右各一条 flank 路
- 上下各一组资源掩体
- 若干树、石头、木箱替换掉工业墙块

这一步先只验证：

- 人物大小是否仍然舒服
- 相机 `1.4` 下视野是否合适
- 掩体间距是否像旧图一样顺手

### 第二步：把白盒障碍物替换成 Fall 风格对象

例如：

- `wall` 替换成大树、巨石、木制 barricade
- `crate` 替换成木箱、南瓜堆、补给堆

但碰撞大小先不变，先保证手感稳定。

### 第三步：再加地面和主题层

把背景从工业风切到：

- 草地底纹
- 泥土过渡
- 林地边缘
- 少量路径/河岸视觉

### 第四步：最后再微调碰撞和遮挡

这一轮才去调整：

- 哪些树应该完全阻挡
- 哪些灌木只做遮挡不做碰撞
- 哪些石头要扩大或缩小 hitbox

## 推荐的第一版范围

为了降低风险，第一版新地图建议限制在下面的范围：

- 不做建筑内部
- 不做门
- 不做复杂河流碰撞
- 不做高低差
- 不做程序生成
- 不做 destructible obstacle

第一版只做：

- 新主题地面
- 新主题障碍物
- 新出生点
- 新拾取点
- 新遮挡表现

这样我们就能先把“风格接入”完成，而不会一下子把地图系统推翻。

## 推荐结论

最终建议可以压缩成一句话：

**保持我们自己的地图尺度、人物尺度和相机视野不变，把 Suroi Fall 当成一套视觉主题和对象库，按 `2560 x 1600` 的竞技场标准重新做一张静态地图。**

如果后面继续推进，实现上最合理的下一步是：

1. 新增 `fall-large` 主题资源映射
2. 扩展障碍物种类
3. 做一张白盒 `fall-large` 静态地图
4. 再把白盒逐步替换成真实 Fall 风格素材

这条路线最稳，也最容易保持“新图像老图一样好读、一样顺手”。

## 关于局部截取参考图

补充一个更贴近当前想法的路线：

你给的这张图：

- [fall.png](D:/26Spring/Typesafe/slay/TPsys/reference/suroi/client/public/img/backgrounds/menu/fall.png)

非常适合作为局部地图样板。

这条路线的核心不是“把这张图直接裁出来放进游戏里”，而是：

**先按这张图选一个局部区域，再把这个区域里的对象关系和空间结构，重建到我们的 `2560 x 1600` 地图坐标里。**

也就是说，真正被“截取”的应该是：

- 场景构图
- 道路走势
- 河流和桥的位置关系
- 树群和石头的分布节奏
- 建筑和掩体的相对位置

而不应该直接截取成：

- 最终碰撞图
- 最终可玩地图贴图

## 为什么这条路现在更适合我们

这个局部图本身已经具备几件很有价值的东西：

1. 它不是超大开放图，而是一个局部战斗片段。
2. 里面已经有桥、河道、建筑、树群、路网和资源点的组合。
3. 它比整张 Suroi `fall` 更接近“竞技场版本”的地图设计。
4. 它很容易挑出一个主战区，再围绕主战区做二次扩展。

所以这条路比“从整张 `fall` 生成图里选区域”更轻，也更符合我们现在的节奏。

## 正确的使用方式

建议把这张图当成一张**布局参考板**，按下面方式处理。

### 1. 先选中核心可玩区域

这张图里比较适合做主战区的结构已经很明显：

- 中左下的房屋和环形小路
- 右侧的河道和桥
- 左上到中部的树群与箱子散点
- 中部偏右的大树群和石头掩体

这意味着我们可以把地图重心理解成：

- 左中区域是近距离拉扯区
- 中部是掩体转移区
- 右侧桥区是高风险通行区
- 河边是边缘战斗区

这个结构是能直接转成我们的竞技地图骨架的。

### 2. 再把对象按我们的尺度放大

既然我们不改：

- 世界大小 `2560 x 1600`
- 相机 `zoom 1.4`
- 角色半径 `18`
- 角色缩放 `1.35`

那么图里的对象就不能按它原始像素关系照搬，而要重新定标。

原则是：

- 建筑轮廓放大到和我们现有主掩体接近
- 树冠放大到能形成明确遮挡和视线阻断
- 石头放大到能承担中型掩体角色
- 桥面宽度按“2 到 3 个角色并行通过”设计
- 河道宽度按“明显构成地形分隔”设计

换句话说：

**保留布局关系，重做尺寸体系。**

### 3. 最后才决定哪些对象进入碰撞

这张参考图里的元素很多，但第一版没必要全进玩法。

推荐分层：

- 强碰撞：
  建筑主体、桥边结构、大树、巨石、木箱
- 弱碰撞或后续决定：
  灌木、小南瓜堆、零散木桶
- 纯装饰：
  小石子、落叶、地表细碎装饰、浅色小花

这样我们能先把视觉和战斗空间搭起来，再慢慢补细节。

## 一个适合我们的落地方式

如果按照你现在这个想法继续推进，我建议实际实施时走下面这条更具体的路线：

1. 以这张 `fall.png` 为参考，手工圈定一个“核心区域布局”。
2. 在我们自己的地图里先画白盒版本：
   - 建筑块
   - 树块
   - 石头块
   - 河道块
   - 桥块
   - 路径块
3. 白盒验证通过后，再把这些白盒替换成 Suroi 风格素材。
4. 最后补出生点、物资点和视线阻挡。

这个流程的好处是：

- 不会被原图像素尺寸绑死
- 不会让碰撞和视觉错位太严重
- 能持续保持旧地图的可读性
- 可以真正做成“像 Suroi，但手感还是我们自己的图”

## 这条路线和直接裁图的区别

如果直接裁图并把裁下来的图当作地图底图来用，会有几个隐患：

- 对象真实尺寸和碰撞尺寸不容易对齐
- 后续如果要挪一棵树或一块石头，会很麻烦
- 桥、河道、建筑、树群都被焊死在一张大图里，迭代成本高
- 前后端无法共享对象级地图信息

如果把它当“布局参考图”来重建，则这些问题都会小很多。

所以更准确地说，你这个思路应该表述为：

**可以截取 Suroi 的局部场景作为地图蓝本，但最终实现要按对象级别重建，不是直接把裁下来的图当成最终地图。**
