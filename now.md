# Now

更新时间：2026-04-29 Asia/Shanghai

## 当前阶段

项目已经从 BattlePage 渲染急救和 `GameScene` 硬解耦，进入“非 BattlePage 素材的产品闭环与扩展性推进”阶段。

当前总原则：
- BattlePage 的角色、地图、箱子、地面等素材暂时冻结，不继续做审美替换；只允许修复明确 bug 或恢复既有手感。
- 继续推进大厅、站内信、好友入口、地图/武器/技能/bot 扩展性、数据闭环、启动与验收脚本。
- 课程代码风格大整理暂缓，等功能闭环更稳定后再集中处理。
- 保持类型安全、声明式边界、前后端 API 同名、可拆微服务方向。

## 当前可以玩到什么程度

已登录账号可以进入 authoritative multiplayer battle，与服务器同步的玩家和 bot 对战。BattlePage 已恢复到原来的 Kenney top-down PNG 玩家风格；滚轮/数字切枪、手枪、火箭炮、加特林、霰弹枪、技能、钩取、击中反馈、火箭 AoE、加特林热量和后坐力都已经进入可玩状态。

对局结束后，基础闭环已经打通：战报、回放、rating/profile、站内信可以生成和读取。Visitor/访客正式开战、写入战绩、写入回放、写入评分等路径已有前后端防护，历史 Visitor-like 脏数据也清理过一轮。

大厅正在从普通功能入口向“金属战争指挥台”方向推进。当前不是最终美术版，但已经有更密集的状态卡、战备信息、邮件/好友/回放摘要和中心 CTA 氛围。

## 最近已完成

- `GameScene` 硬解耦完成，并生成 `docs/GAMESCENE_HARD_GATE_COMPLETION_REPORT.md`。
- BattlePage 渲染与手感完成阶段性收口，当前不再继续替换素材。
- 新增 `npm run dev:status` 与 `npm run dev:start`，可诊断并启动缺失的前后端服务，不主动杀进程。
- 好友入口第一轮接入真实 friend request preview，badge 只统计当前用户收到的待处理请求。
- 站内信第二轮优化完成：概览统计、战报筛选、合并战报提示、批量标记已读、失败回滚。
- 大厅视觉结构第二轮已接收：新增“战备指挥台 / Operation Deck”，展示账号在线状态、评分、未读邮件、好友待处理、最近回放等真实数据。
- 扩展性第二轮已接收：新增社区 bot 包 manifest 示例、离线 package audit、README/SDK 文档说明。外部贡献者现在可以按 `*.plugin.json + .mjs strategy` 的格式提交 bot，审计脚本会校验字段、权限、entry、导出策略和 `strategyId` 声明，不需要改核心战斗代码。
- 扩展性第三轮已接收：`audit:battle-contracts` 现在除了比较前后端 catalog 值相等，还会校验地图边界、ID 唯一性、拾取物引用、武器数值范围、热量武器字段、技能 key/effect/activation 必填字段，并在失败时输出明确路径。

## 当前风险与注意事项

- 如果 8080 已有 `BackendApp` 运行，再执行 `sbt run` 出现 `Address already in use` 是端口占用，不等于后端代码损坏。
- 如果 5173 有多个 Vite listener，说明前端可能被重复启动过；优先用 `npm run dev:status` 判断状态。
- BattlePage 素材方向目前锁住，避免再次偏离用户审美预期。
- 课程代码风格、Scala `var/val`、DTO/Parser/Renderer 风格整理会影响面较大，暂时不主动推进。

## 下一步计划

1. 大厅视觉结构第三轮，预计 3-6 小时。
   目标：继续靠近参考图的金属战争大厅结构，强化榜单、玩家卡、邮件/好友入口和中心开始区的版面密度。仍然只使用程序化 CSS 与现有资产，不碰 BattlePage 素材。

2. 数据闭环第二轮，预计 2-4 小时。
   目标：继续查 duplicate projection、多账号/多标签页、同账号异地登录、rating/profile 派生一致性；优先做审计和防护，不急着重构账号系统。

3. 站内信/好友后续，预计 3-6 小时。
   目标：把好友消息的模型边界、实时同步策略和 UI 入口设计清楚；真正聊天系统可以后置，不在当前轮次强行铺大。

4. 课程代码风格整理，暂缓。
   目标：等用户确认老师要求后，再统一处理不可变性、模块边界、DTO 契约、前后端 API 同名和微服务拆分形式。

## 当前执行策略

继续按“一张票、一名 worker、一轮审查、一个合并决定”的方式推进。下一张票优先做大厅视觉结构第三轮或数据闭环第二轮；两者都不会触碰 BattlePage 素材，前者提高商业化呈现，后者提高战报/rating/profile 的可信度。
