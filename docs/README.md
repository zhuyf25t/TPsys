# Docs Index

更新时间：2026-04-30 Asia/Shanghai

当前文档已按阶段归档到 `docs/phases/`。新 Codex 不应从旧散乱文档开始读，而应先读：

1. 根目录 `AGENTS.md`：用户会覆盖为新的编码规则，这是最高优先级。
2. 根目录 `now.md`：当前真实状态。
3. `docs/phases/phase-05-backend-rewrite/BACKEND_REWRITE_HANDOFF.md`：后端重写交接。
4. `docs/phases/phase-05-backend-rewrite/NEXT_CODEX_BACKEND_REWRITE_PROMPT.md`：可直接复制给新 Codex 的启动 prompt。

## Phase Folders

- `phase-01-foundation-and-product/`：早期产品、前端、菜单与体验规划。
- `phase-02-gamescene-hard-decoupling/`：`GameScene` 硬解耦历史与完成报告。
- `phase-03-battle-rendering-authoritative/`：BattlePage、authoritative battle、渲染与实时对战历史。
- `phase-04-product-data-extension/`：数据闭环、bot SDK、content contract、产品扩展性。
- `phase-05-backend-rewrite/`：当前阶段，重写整个后端。

`docs/notes/` 保留原始笔记，不作为新 Codex 的第一阅读入口。
